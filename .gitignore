# Bỏ qua MySQL data (rất nặng)
mysql/

# KHÔNG bỏ qua source code WordPress
# Nếu bạn muốn giữ code WordPress, nhưng không muốn commit wp-content/uploads (ảnh)
wordpress/wp-content/uploads/

# Logs và file tạm
*.log
*.tmp
*.swp

# Backup / dump
*.sql
*.sql.gz

# Hệ thống (Windows / macOS)
.DS_Store
Thumbs.db
desktop.ini

# Node / npm / composer cache (phòng khi dùng thêm)
node_modules/
vendor/

# Env files (chứa password, secret)
.env
.env.*
